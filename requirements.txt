# Python 3.14 compatible — no old SDKs, pure REST client architecture
# Install: pip install -r requirements.txt

# HTTP (REST client for Fabric Gateway API + IPFS)
requests>=2.32.0

# Cryptography (ECDSA signing, X.509)
cryptography>=42.0.0

# Config / env
PyYAML>=6.0.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
