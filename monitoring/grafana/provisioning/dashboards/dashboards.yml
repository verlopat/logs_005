apiVersion: 1
providers:
  - name: security-logger
    orgId: 1
    folder: Security Logger
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
